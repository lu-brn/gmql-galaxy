<tool id="gmlq_rest_datasets_samples" name="GMLQ Datasets Samples" version="0.1.1">
  <description>List the samples of a given dataset.</description>
  <command><![CDATA[
       python $__tool_directory__/gmql_rest_datasets.py $output -user=$authToken -cmd=samples -dataset=${dataset.ds_name} -owner=${dataset.owner}
  ]]></command>
  <inputs>
	<param name="authToken" type="data" format="json" label="Select User"/>
    <param name="gmql_datasets" type="data" format="gmql_repository" label="Working Datasets" />
	<conditional name="dataset" >
        <param name="owner" type="select" label="Private or public dataset" display="radio" multiple="false">
            <option value="user">Private</option>
            <option value="public">Public</option>
        </param>
        <when value="user">
             <param name="ds_name" type="select" label="Select Dataset">
		        <options from_dataset="gmql_datasets">
                     <column name="value" index="0" />
                    <filter column="1" type="static_value" value="public" keep="false"/>
		        </options>
	         </param>
        </when>
        <when value="public">
            <param name="ds_name" type="select" label="Select Dataset">
		        <options from_dataset="gmql_datasets">
                     <column name="value" index="0" />
                     <filter column="1" type="static_value" value="public" />
 		        </options>
	         </param>
        </when>
    </conditional>
  </inputs>
  <outputs>
    <data format="tabular" name="output" label="${dataset.ds_name} samples" >
        <actions>
            <action name="column_names" type="metadata" default="id,sample" />
        </actions>
    </data>
  </outputs>
  <help>
  **What it does**
  
  It retrieves the samples of the given dataset.
  
  </help>
</tool>
