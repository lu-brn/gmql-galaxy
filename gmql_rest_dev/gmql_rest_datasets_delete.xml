<tool id="gmlq_rest_datasets_delete" name="GMLQ Delete Dataset" version="0.1.0">
  <description>Delete a given dataset from the user's working space.</description>
  <command interpreter="python">gmql_rest_datasets.py $output -user=$authToken -cmd=delete -dataset=$dataset
	  </command>
  <inputs>
	<param name="authToken" type="data" format="json" label="Select User"/>
	<param name="gmql_datasets" type="data" format="tabular" label="Working Datasets" />
	<param name="dataset" type="select" label="Select Dataset">
		<options from_dataset="gmql_datasets">
			 <column name="value" index="0"/>			
		</options>
	</param>
	</inputs>
  <outputs>
    <data format="tabular" name="output" label="Outcome" />
  </outputs>
  <help>
  **What it does**
  
  Delete from the user's working space the given dataset. Note that only datasets in the private area can be deleted. 
  
  </help>	
	
</tool>

