---
STATEMENT:
    '{o_variable} = {operator}({parameters}) {i_variables} ;'
MATERIALIZE:
    'MATERIALIZE {variable} INTO {file_name};'
PARAMETERS:
    select:
        metadata: '{p}'
        region: 'region: {p}'
        semijoin: 'semijoin: {p}'